#include "main.h"
/**
* _memset - fills memory with a constant byte
*@n: number of bytes to fill
*@s: the pointer to memory area to fill
*@b: constant byte
*Return: pointer to the memory area s
*/

char *_memset(char *s, char b, unsigned int n);
{
          unsigned int n;
          n=i;
          
          for(i = 0; i < n; i ++)
          {
          s[i] =b;
          }
          return(s)
}
